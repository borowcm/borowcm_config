;; This file never changes

(setq user-name (user-original-login-name))
(defun USER-load-file (f) (load-file
			   (concat "/home/LOGIN/emacs/customizations/" f)))

;:*========                 P A T H S                           ========

(USER-load-file "paths.el")

;:*========                 V A R I A B L E S                   ========

(USER-load-file "variables.el")

;:*========                 K E Y S                             ========

(USER-load-file "keys.el")

;:*========                 P A C K A G E S                     ========

(USER-load-file "load-packages.el")

;:*========                 C and C++  S E T T I N G S          ========

(USER-load-file "c-and-c++.el")

;:*========                 P E R L   S E T T I N G S           ========

(USER-load-file "perl-settings.el")

;:*========                 Calendar and Diary settings         ========

(USER-load-file "calendar-diary.el")

;:*========                 USER DEFINED MACROS                 ========

(USER-load-file "userdef-macros.el")

;:*========                 L a T e X    S E T T I N G S        ========

(USER-load-file "latex-settings.el")

;:*========                 V E R I L O G                       ========

(USER-load-file "verilog-mode.el")

;; Vijay's emacs settings.

